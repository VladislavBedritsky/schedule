<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://camel.apache.org/schema/spring
        http://camel.apache.org/schema/spring/camel-spring.xsd">

    <import resource="classpath*:test.xml" />

    <camel:camelContext id="produceDataToScheduleQueue">
        <camel:route>
            <camel:from uri="direct:start"/>
            <camel:to uri="activemq:queue:schedule_queue" />
        </camel:route>
    </camel:camelContext>

    <camel:camelContext id="consumeDataFromScheduleQueue">
        <camel:route >
            <camel:from uri="activemq:queue:schedule_queue"/>
            <camel:to uri="seda:end" />
        </camel:route>
    </camel:camelContext>

    <bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
        <property name="connectionFactory">
            <bean class="org.apache.activemq.ActiveMQConnectionFactory">
                <property name="brokerURL" value="${activemq.brokerUrl}"/>
                <property name="password" value="${activemq.username}" />
                <property name="userName" value="${activemq.password}" />
            </bean>
        </property>
    </bean>

    <bean id="producerService" class="org.kek.camel.service.impl.ProducerServiceImpl" />
    <bean id="consumerService" class="org.kek.camel.service.impl.ConsumerServiceImpl" />
</beans>
